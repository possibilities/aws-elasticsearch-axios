{
  "name": "aws-elasticsearch-axios",
  "license": "MIT",
  "version": "0.0.0",
  "scripts": {
    "test": "jest --verbose --runInBand",
    "lint": "standard"
  },
  "dependencies": {
    "aws4": "^1.8.0",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "globals": [
      "test",
      "expect",
      "jest",
      "beforeEach",
      "afterEach",
      "describe"
    ]
  },
  "jest": {
    "testEnvironment": "node"
  }
}
